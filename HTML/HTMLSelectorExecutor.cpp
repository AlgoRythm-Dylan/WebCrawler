#include "HTMLSelectorExecutor.h"

vector<shared_ptr<HTMLNode>> execute_selector(shared_ptr<HTMLNode> search_root, const HTMLSelector& selector)
{
	vector<shared_ptr<HTMLNode>> results;
	// Walk the node tree and recursively add nodes to the result set
	return results;
}

void execute_selector_step(shared_ptr<HTMLNode> search_node, const HTMLSelector& selector, vector<shared_ptr<HTMLNode>>& result_set)
{

}